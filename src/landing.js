import { Outlet, Link } from "react-router-dom";
import { useState } from "react";
import Counter from "./useStates.js"
import ShowCount from "./useStates.js";

const Landing = () => {

    return(
        <div>
        <h1>This is the Week 5 Assignment.</h1>

        <p>Each page covers a different part of the week's Assignment. </p>
        <br></br>
        <p>The navigation of this page is handled with a React Router!</p>
        </div>
    );






};

export default Landing;
